name: fireredvad
description: Streaming Voice Activity Detection + Audio Event Detection using DFSMN. Pure Dart implementation.
version: 0.1.0
repository: https://github.com/voxcom-us/fireredvad-dart

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
